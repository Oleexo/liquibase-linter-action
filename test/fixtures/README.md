# Liquibase Linter Action - Test Fixtures

This directory contains test fixtures for CI/CD testing.

## Directories

- `no-violations/` - Clean changelog files with no linting violations
- `with-violations/` - Changelog files with intentional violations for testing

## Usage

These fixtures are used by the GitHub Actions workflows in `.github/workflows/` to verify the action works correctly.
